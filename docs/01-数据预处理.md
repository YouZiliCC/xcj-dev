# 数据预处理与解析

> 实现说明 / Implementation Note
>
> 本章是早期设计稿，记录预处理的算法思路（状态机抽取「研究设计」、滑动窗口切块）。这些思路在工程实现中完整保留，但落地细节与本文有以下差异，阅读时请以本说明为准：
>
> | 本章设计（早期） | 当前真实实现 | 说明 / 理由 |
> | --- | --- | --- |
> | 输入元数据为 JSON | 万方导出的 `data/WanFangdata.csv`（GBK/chardet 探测编码） | 数据源即为 CSV，无独立 JSON |
> | 输出至 MySQL + ChromaDB | 统一写入单个 SQLite（`papers_master` + `paper_chunks`） | 单机零依赖，详见 02 章实现说明 |
> | 向量由 BERT/BGE-Large 生成 | 本地 `BAAI/bge-small-zh-v1.5`（sentence-transformers，512 维） | 小模型即可满足课程规模，下载快 |
> | —（未明确全文字段） | 预处理拼接连续全文写入 `papers_master.raw_body`（详情页 `full_text` 直接复用，见 06 章 T1） | 支撑详情页连续原文阅读 |
> | —（未明确分词对齐） | 抽取阶段同时产出与 Go `search.Tokenize` 等价的 `*_tokens`，并把向量按 float32 LE → base64 → BLOB 落库 | 后端无需重复分词，编解码对称 |
>
> 算法层面：状态机章节抽取与本章一致；**2026-06 更新**：切块已改为两阶段语义切分——① 章节标题正则切分（第X章/一、/1 …，含目录检测与序号校验），整章标注段落作用 `section_role ∈ {概念解释,背景说明,方法依据,经验证据,研究空白,其他}`；② BGE 句向量边界检测（均值-1 标准差谷点）+ 语义段贪心打包至约 500 字（硬上限 620）；并过滤万方水印/页码/基金项目脚注等噪声段。滑窗仅以 `--split-method legacy` 保留。

## 1. 输入源 (Input Sources)

系统接收两种类型的输入数据：

-   **外部基础元数据 (JSON)**
    -   内容：DOI、标题、关键词等
    -   绑定方式：通过文件名/映射表
-   **1000篇原生Word文献 (.docx)**
    -   结构：标准学术论文结构排版
    -   输入方式：输入文件流

## 2. 处理核心 (Processing Core)

**核心引擎：python-docx 引擎**

-   **模块 1：全文读取与结构扫描 (Document Parser)**
    -   功能：解析 Word 文档，读取全文并识别基本结构。

经过核心处理后，数据流分为两个分支：

## 3. 流向分支 A：定向章节切片

-   **模块 2：学术章节状态机检测器 (Regex State Machine)**
    -   **触发开关**：检测“研究设计”等章节起始标记。
    -   **截止开关**：检测“模型分析”等章节结束标记。
-   **输出产物**：**粗粒度整段文本对象**
    -   示例：`research_design_text` (包含完整的研究设计章节内容)

## 4. 流向分支 B：全文滚动切块

-   **模块 3：滑动窗口文本切分器 (Sliding Window)**
    -   **窗口大小**：500 字
    -   **重叠大小**：100 字
-   **输出产物**：**二维映射文本块集合**
    -   结构：`[chunk_id, text, index]`

> 2026-06 更新：滑动窗口切分已被两阶段语义切分取代（见顶部实现说明），本节仅保留为早期设计记录。

## 5. 统一数据封装层 (Data Packaging)

两个分支的输出在此汇合并整合：

-   **输出格式**：标准增强型论文数据对象 (Unified JSON Schema)

## 6. 最终输出 (Final Outputs)

封装后的数据分别输送至不同的存储模块：

-   **输出至 MySQL**：存入学术主表及设计大字段 (存储结构化元数据和完整章节文本)
-   **输出至 ChromaDB**：先由 BERT 模型将文本转化为向量，再存入向量库 (用于语义检索和相似度匹配)

## 7. 数据流总结图 (文本简化版)

```text
[输入源]
   ├── JSON元数据 ──────┐
   │                    │ (文件名/映射表绑定)
   └── 1000篇.docx ─────┘
            │
            ▼
[python-docx 引擎]
   └── 全文读取与结构扫描
            │
     ┌──────┴──────┐
     │             │
     ▼             ▼
[定向章节(研究设计)切片]    [全文滚动切块]
   │               │
   ▼               ▼
[状态机检测]      [滑动窗口]
(触发/截止开关)   (500/100字)
   │               │
   ▼               ▼
[(研究设计)相关整段文本]        [文本块集合]
   │               │
     └──────┬──────┘
            │
            ▼
[统一数据封装] (JSON)
            │
     ┌──────┴──────┐
     │             │
     ▼             ▼
   MySQL         ChromaDB
(主表+设计大字段) (BERT向量)